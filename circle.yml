dependencies:
  override:
    - npm install -g firebase-tools
deployment:
  deploy:
    branch: master
    commands:
      - npm run build && firebase deploy --project "$FIREBASE_PJ" --token "$FIREBASE_TOKEN"